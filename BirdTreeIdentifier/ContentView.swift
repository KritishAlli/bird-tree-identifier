//
//  ContentView.swift
//  BirdTreeIdentifier
//
//  Created by Kritish Alli on 2/14/26.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, hello!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
